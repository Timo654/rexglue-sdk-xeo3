// Generated by rexglue
// DO NOT EDIT - this file is auto-generated
#pragma once

//=============================================================================
// Project Configuration
//=============================================================================

#define REX_CONFIG_H_INCLUDED

{% if config_flags.skip_lr %}#define REX_CONFIG_SKIP_LR
{% endif %}{% if config_flags.ctr_as_local %}#define REX_CONFIG_CTR_AS_LOCAL
{% endif %}{% if config_flags.xer_as_local %}#define REX_CONFIG_XER_AS_LOCAL
{% endif %}{% if config_flags.reserved_as_local %}#define REX_CONFIG_RESERVED_AS_LOCAL
{% endif %}{% if config_flags.skip_msr %}#define REX_CONFIG_SKIP_MSR
{% endif %}{% if config_flags.cr_as_local %}#define REX_CONFIG_CR_AS_LOCAL
{% endif %}{% if config_flags.non_argument_as_local %}#define REX_CONFIG_NON_ARGUMENT_AS_LOCAL
{% endif %}{% if config_flags.non_volatile_as_local %}#define REX_CONFIG_NON_VOLATILE_AS_LOCAL
{% endif %}
#define REX_IMAGE_BASE {{ image_base }}ull
#define REX_IMAGE_SIZE {{ image_size }}ull
#define REX_CODE_BASE {{ code_base }}ull
#define REX_CODE_SIZE {{ code_size }}ull
#define REX_THUNK_RESERVE_SIZE {{ thunk_reserve_size }}ull

#define REXCRT_HEAP {% if rexcrt_heap %}1{% else %}0{% endif %}

//=============================================================================
// SDK Headers
//=============================================================================

#include <rex/ppc.h>
#include <rex/image_info.h>
//#include <rex/logging.h>
//#include <rex/chrono/clock.h>
//#include <rex/perf/counter.h>
//#include <rex/thread/mutex.h>

#include <atomic>
#include <cassert>
#include <csetjmp>
#include <cstdlib>
#include <stdexcept>
#include <string_view>
#include <unordered_map>

#ifndef __XEO3_PRESERVE_NONE__
#error "Clang fork using modified preserve_none calling convention is required to compile this code."
#endif

//=============================================================================
// Image Info
//=============================================================================

extern const rex::PPCImageInfo PPCImageConfig;
extern PPCFuncMapping PPCFuncMappings[];

//=============================================================================
// Function Definition Macros
//=============================================================================

#define DECLARE_REX_FUNC(name) \
  REX_EXTERN(name);            \
  REX_EXTERN(__imp__##name)

#define DEFINE_REX_FUNC(name)                                  \
  __attribute__((alias("__imp__" #name))) REX_WEAK_FUNC(name); \
  REX_EXTERN(__imp__##name)

//=============================================================================
// Function Prologue
//=============================================================================

#if defined(REXGLUE_PROFILE_GUEST_FUNCTIONS) && defined(REXGLUE_ENABLE_PROFILING)
#include <tracy/Tracy.hpp>
#if defined(__clang__)
#define REX_FUNC_PROLOGUE()                     \
  __builtin_assume(((size_t)base & 0x1F) == 0); \
  ZoneNamedN(___tracy_guest_zone, __func__, true)
#elif defined(__GNUC__)
#define REX_FUNC_PROLOGUE()         \
  do {                              \
    if (((size_t)base & 0x1F) != 0) \
      __builtin_unreachable();      \
  } while (0);                      \
  ZoneNamedN(___tracy_guest_zone, __func__, true)
#else
#define REX_FUNC_PROLOGUE() ZoneNamedN(___tracy_guest_zone, __func__, true)
#endif
#else
#if defined(__clang__)
#define REX_FUNC_PROLOGUE() __builtin_assume(((size_t)base & 0x1F) == 0)
#elif defined(__GNUC__)
#define REX_FUNC_PROLOGUE()         \
  do {                              \
    if (((size_t)base & 0x1F) != 0) \
      __builtin_unreachable();      \
  } while (0)
#else
#define REX_FUNC_PROLOGUE() ((void)0)
#endif
#endif

//=============================================================================
// Memory Access
//=============================================================================

#define REX_PHYS_HOST_OFFSET(addr) 0u

#define REX_RAW_ADDR(x) (base + (u32)(x))

#define REX_LOAD_U8(x) (*(volatile u8*)(base + (u32)(x) + REX_PHYS_HOST_OFFSET(x)))
#define REX_LOAD_U16(x) __builtin_bswap16(*(volatile u16*)(base + (u32)(x) + REX_PHYS_HOST_OFFSET(x)))
#define REX_LOAD_U32(x) __builtin_bswap32(*(volatile u32*)(base + (u32)(x) + REX_PHYS_HOST_OFFSET(x)))
#define REX_LOAD_U64(x) __builtin_bswap64(*(volatile u64*)(base + (u32)(x) + REX_PHYS_HOST_OFFSET(x)))
#define REX_LOAD_STRING(x, len) \
  std::string_view(reinterpret_cast<const char*>(base + (u32)(x) + REX_PHYS_HOST_OFFSET(x)), (len))

#define REX_STORE_U8(x, y)  (*(volatile u8*)(base + (u32)(x) + REX_PHYS_HOST_OFFSET(x)) = (y))
#define REX_STORE_U16(x, y) (*(volatile u16*)(base + (u32)(x) + REX_PHYS_HOST_OFFSET(x)) = __builtin_bswap16(y))
#define REX_STORE_U32(x, y) (*(volatile u32*)(base + (u32)(x) + REX_PHYS_HOST_OFFSET(x)) = __builtin_bswap32(y))
#define REX_STORE_U64(x, y) (*(volatile u64*)(base + (u32)(x) + REX_PHYS_HOST_OFFSET(x)) = __builtin_bswap64(y))

#define REX_MEMORY_SIZE 0x100000000ull

//=============================================================================
// MMIO
//=============================================================================

#define REX_IS_MMIO_ADDR(addr) ((addr) >= 0x7F000000u && (addr) < 0x80000000u)

#define REX_MM_STORE_U8(addr, val) REX_STORE_U8 (addr, val)

#define REX_MM_STORE_U16(addr, val) REX_STORE_U16(addr, val)

#define REX_MM_STORE_U32(addr, val) REX_STORE_U32(addr, val); // FIXME - fix and use emu functions

#define REX_MM_STORE_U64(addr, val) REX_STORE_U64(addr, val)

#define REX_MM_LOAD_U8(addr) REX_LOAD_U8 (addr)

#define REX_MM_LOAD_U16(addr) REX_LOAD_U16(addr)

#define REX_MM_LOAD_U32(addr) REX_LOAD_U32(addr) // FIXME - fix and use emu functions

#define REX_MM_LOAD_U64(addr) REX_LOAD_U64(addr)

//=============================================================================
// Function Dispatch
//=============================================================================

#define REX_CALL_FUNC(x) x(ctx, base, frame)

{% include "codegen/_indirect_call.inja" %}

//=============================================================================
// Flush Mode
//=============================================================================

#define REX_SET_FLUSH_MODE(enable) \
  do { if (enable) ADJ(ctx)->fpscr.enableFlushMode(); else ADJ(ctx)->fpscr.disableFlushMode(); } while (0)

//=============================================================================
// Misc
//=============================================================================

#define REX_UNIMPLEMENTED(addr, opcode) \
  throw std::runtime_error("Unimplemented PPC instruction: " opcode)

#define REX_QUERY_TIMEBASE() QueryTimebase();

__declspec(dllexport) extern "C" PPCImportMapping PrecompiledImportTable[];
__declspec(dllexport) extern "C" uintptr_t PrecompiledPointers[];
extern "C" uint64_t QueryTimebase();
//=============================================================================
// Setjmp/Longjmp
//=============================================================================

inline std::unordered_map<u32, jmp_buf>& get_jmp_buf_map() {
  static thread_local std::unordered_map<u32, jmp_buf> map;
  return map;
}

#define ppc_setjmp(guest_buf_addr) (setjmp(::get_jmp_buf_map()[(guest_buf_addr)]))

[[noreturn]] inline void ppc_longjmp(u32 guest_buf_addr, int val) {
  auto& map = get_jmp_buf_map();
  auto it = map.find(guest_buf_addr);
  if (it != map.end()) {
    longjmp(it->second, val);
  }
  std::abort();
}

//=============================================================================
// Trap Handling
//=============================================================================

inline void ppc_trap(PPCContext& ctx, u8* base, u16 trap_type) {
  switch (trap_type) {
    case 20:
    case 26: {
      auto str = REX_LOAD_STRING(ctx.r3.u32, ctx.r4.u16);
      //REXCPU_DEBUG("(service trap) {}", str);
      break;
    }
    case 0:
    case 22:
      //REXCPU_WARN("tw/td trap hit (type {})", trap_type);
      break;
    case 25:
      break;
    default:
      //REXCPU_WARN("Unknown trap type {}", trap_type);
      break;
  }
}

// TEMP WORKAROUND
inline void ppc_trap(uintptr_t ctx_addr, uintptr_t base, u16 trap_type) {
    ppc_trap(
        *reinterpret_cast<PPCContext*>(ctx_addr),
        reinterpret_cast<u8*>(base),
        trap_type
    );
}

//=============================================================================
// Interrupt Locks
//=============================================================================

inline std::atomic<i32>& ppc_global_lock_count_() {
  static std::atomic<i32> count{0};
  return count;
}

#define REX_CHECK_GLOBAL_LOCK()                                        \
  ([&]() -> u64 {                                                      \
    auto lock_ = rex::thread::global_critical_region::AcquireDirect(); \
    return ppc_global_lock_count_().load() ? 0 : 0x8000;               \
  }())

#define REX_ENTER_GLOBAL_LOCK()                          \
  do {                                                   \
    rex::thread::global_critical_region::mutex().lock(); \
    ppc_global_lock_count_().fetch_add(1);               \
  } while (0)

#define REX_LEAVE_GLOBAL_LOCK()                                                           \
  do {                                                                                    \
    auto old_count_ = ppc_global_lock_count_().fetch_sub(1);                              \
    assert(old_count_ >= 1 && "LeaveGlobalLock called without matching EnterGlobalLock"); \
    rex::thread::global_critical_region::mutex().unlock();                                \
  } while (0)

//=============================================================================
// Function Declarations
//=============================================================================

{% for fn in functions %}DECLARE_REX_FUNC({{ fn.name }});
{% endfor %}
